<div>
  <div class="mb-6">
    <%= link_to '&larr; Back to Plan'.html_safe, admin_subscription_plan_path(@subscription_plan), class: 'text-sm text-gray-500 hover:text-gray-700' %>
  </div>

  <h1 class="text-2xl font-semibold text-gray-900">Edit <%= @subscription_plan.name %></h1>

  <div class="mt-8 max-w-2xl">
    <%= render 'form', subscription_plan: @subscription_plan %>
  </div>
</div>
